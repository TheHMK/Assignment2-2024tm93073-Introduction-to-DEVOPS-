name: CI Pipeline for ACEest Fitness App

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: ✅ Checkout repository
        uses: actions/checkout@v3

      - name: ✅ Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: ✅ Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: ✅ Run unit tests with pytest
        run: |
          pytest -v

  # OPTIONAL: Docker Build Stage (Uncomment if required)
  # docker-build:
  #   needs: build-and-test
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: ✅ Checkout repository
  #       uses: actions/checkout@v3
  #
  #     - name: ✅ Login to DockerHub
  #       uses: docker/login-action@v2
  #       with:
  #         username: ${{ secrets.DOCKERHUB_USERNAME }}
  #         password: ${{ secrets.DOCKERHUB_PASSWORD }}
  #
  #     - name: ✅ Build and Push Docker Image
  #       run: |
  #         docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/aceest-fitness-app:v1.3 .
  #         docker push ${{ secrets.DOCKERHUB_USERNAME }}/aceest-fitness-app:v1.3
